@model LoginVerificationViewModel
@{
    Layout = "Main";
    ViewData["Title"] = "Verify e-mail";
}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-12 col-sm-10 col-md-8 col-lg-6">
            <form method="post" action="@Url.Action(nameof(UserController.VerifyPerform), "User")" class="big-form">
                <h1>Registrati alla Piattaforma&nbsp;WOM</h1>

                <input type="hidden" name="userId" value="@Model.UserId" />
                <input type="hidden" name="token" value="@Model.Token" />

                <p>Clicca sul pulsante qua sotto per validare il tuo indirizzo e fare login.</p>

                <button type="submit" class="btn btn-primary">Procedi</button>
            </form>
        </div>
    </div>
</div>
