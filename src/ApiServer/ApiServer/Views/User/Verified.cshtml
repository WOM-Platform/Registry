@model LoginViewModel
@{
    Layout = "Main";
    ViewData["Title"] = "Register";
}

<div class="container">
    <div class="row justify-content-center">
        <div class="col-12 col-sm-10 col-md-8 col-lg-6">
            <form method="get" class="big-form">
                <h1>Register to the WOM&nbsp;Platform</h1>

                <p>Your e-mail address has been successfully verified!</p>
                <p><a href="@Url.Action(nameof(DashboardController.Index), "Dashboard")">Go to your merchant dashboard</a></p>
            </form>
        </div>
    </div>
</div>
