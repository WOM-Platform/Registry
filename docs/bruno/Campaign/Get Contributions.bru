meta {
  name: Get Contributions
  type: http
  seq: 1
}

get {
  url: GET {{protocol}}://{{baseUrl}}/v1/campaign/{{campaignId}}/contributions
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
