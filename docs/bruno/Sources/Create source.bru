meta {
  name: Create source
  type: http
  seq: 1
}

post {
  url: {{protocol}}://{{baseUrl}}/api/v1/source
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "name": "Test source",
    "url": "https://example.org/",
    "aims": {
      "enabled": ["C", "K"],
      "enableAll": true
    }
  }
}
