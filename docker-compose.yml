version: '3'

networks:
  web:
    external: true
  backend:

services:
  api:
    build: ./src/ApiServer
    depends_on:
    - mongo
    networks:
    - web
    - backend
    env_file: config.env
    expose:
    - "8779"

  mongo:
    image: mongo:4.4.12
    networks:
    - backend
    expose:
    - "27017"
    env_file: config.env
